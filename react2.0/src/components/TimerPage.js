import React from 'react';
import { Timer } from './Timer';

export default function TimerPage() {
    return (
        <div className="container">
            <h1 className="text-center mt-4">Timer Page</h1>
            <Timer />
        </div>
    );
}